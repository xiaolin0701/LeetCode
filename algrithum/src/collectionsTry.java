package src;

import java.util.*;
import java.util.concurrent.LinkedBlockingQueue;

public class collectionsTry {
    void newCollectionTry(){
        Set < Object > set=new HashSet<>();
        List<Object> list=new ArrayList<>();
        Queue<Object> queue=new LinkedBlockingQueue<>();
    }
}
